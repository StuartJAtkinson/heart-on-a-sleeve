# Considerations

- Alembic is set up (`backend/alembic/`, baseline migration `f652273fd930`, verified by `backend/tests/test_alembic_migrations.py`), and `backend/app/core/migrations.py` now has a `run_migrations()` that safely runs `alembic upgrade head` against either a fresh DB or one already bootstrapped by the old `create_all` path (it stamps to baseline first instead of re-running `CREATE TABLE` on existing tables — covered by `backend/tests/test_migrations_runner.py`, both scenarios pass). It's wired into `router.py`'s `lifespan()` but gated behind `Settings.use_alembic_migrations` (env var `USE_ALEMBIC_MIGRATIONS`), **default `False`**, so production behaviour is unchanged until a human flips it. Decided 2026-08-02: leave gated — verify against a copy/staging of the production Postgres/PostGIS DB before flipping `USE_ALEMBIC_MIGRATIONS=true` in the live Cloud Run env; the local tests only cover sqlite.
